# @your-scope/backstage-plugin-aws-devops-agent

## 0.1.0

### Minor Changes

- Initial release. Adds a per-component **DevOps Agent** entity tab (new frontend
  system) showing AWS DevOps Agent recommendations and investigations, an
  "Ask the agent" chat, and a "Start investigation" action. Gated on the
  `aws-devops-agent/space-id` annotation. Requires the companion backend plugin.
